// AUTO-GENERATED by scripts/generate-api.mjs — DO NOT EDIT.
// Regenerate with `npm run codegen`.

import { GeneratedResource, e } from './runtime.js'

/**
 * marketplace — 33 operations (5 destructive).
 *
 * Generated from `specs/marketplace.json`.
 *
 * 0 operation(s) return a described shape; the other 33 return `unknown`
 * because their catalog declares no response schema. Every method still takes a
 * type argument, so a caller can name a shape the spec doesn't know:
 * `listBoards<{ data: Board[] }>()`.
 */
export class MarketplaceApi extends GeneratedResource {
  /** Create an email sender — @write */
  createEmailSender<T = unknown>(params: { body: { name: string; email: string } }): Promise<T> {
    return this.req<T>("POST", `/apps/email-senders`, undefined, params.body)
  }

  /** Create an email template — @write */
  createEmailTemplate<T = unknown>(params: { body: { name: string; subject?: string; body?: string; board_id?: string } }): Promise<T> {
    return this.req<T>("POST", `/apps/email-templates`, undefined, params.body)
  }

  /** Create a form — @write */
  createForm<T = unknown>(params: { body: { name: string; fields: unknown[]; app_id?: string; teams?: unknown[] } }): Promise<T> {
    return this.req<T>("POST", `/apps/forms`, undefined, params.body)
  }

  /** ⚠️ DESTRUCTIVE — Delete an email sender — @destructive */
  deleteEmailSender<T = unknown>(params: { id: string }): Promise<T> {
    return this.req<T>("DELETE", `/apps/email-senders/${e(params.id)}`, undefined, undefined)
  }

  /** ⚠️ DESTRUCTIVE — Delete an email template — @destructive */
  deleteEmailTemplate<T = unknown>(params: { id: string }): Promise<T> {
    return this.req<T>("DELETE", `/apps/email-templates/${e(params.id)}`, undefined, undefined)
  }

  /** ⚠️ DESTRUCTIVE — Delete a file — @destructive */
  deleteFile<T = unknown>(params: { id: string }): Promise<T> {
    return this.req<T>("DELETE", `/files/${e(params.id)}`, undefined, undefined)
  }

  /** ⚠️ DESTRUCTIVE — Delete a form — @destructive */
  deleteForm<T = unknown>(params: { id: string }): Promise<T> {
    return this.req<T>("DELETE", `/apps/forms/${e(params.id)}`, undefined, undefined)
  }

  /** ⚠️ DESTRUCTIVE — Detach a team from all its forms (reassigns to General) — @destructive */
  detachTeamForms<T = unknown>(params: { teamId: string }): Promise<T> {
    return this.req<T>("DELETE", `/apps/forms/team/${e(params.teamId)}`, undefined, undefined)
  }

  /** Get one app instance by id */
  getAppById<T = unknown>(params: { id: string }): Promise<T> {
    return this.req<T>("GET", `/apps/${e(params.id)}`, undefined, undefined)
  }

  /** Aggregate app counts by status */
  getAppStatusCounts<T = unknown>(): Promise<T> {
    return this.req<T>("GET", `/apps/status`, undefined, undefined)
  }

  /** Get the board template for a board */
  getBoardTemplate<T = unknown>(params: { boardId: string }): Promise<T> {
    return this.req<T>("GET", `/apps/utils/board-template/${e(params.boardId)}`, undefined, undefined)
  }

  /** Get a built-in assistant catalog entry */
  getBuiltinAssistant<T = unknown>(params: { assistant_id: string }): Promise<T> {
    return this.req<T>("GET", `/use-cases/builtin-assistants/${e(params.assistant_id)}`, undefined, undefined)
  }

  /** Get the default form template schema */
  getDefaultFormTemplate<T = unknown>(): Promise<T> {
    return this.req<T>("GET", `/apps/forms/template`, undefined, undefined)
  }

  /** Get an email sender by id */
  getEmailSenderById<T = unknown>(params: { id: string }): Promise<T> {
    return this.req<T>("GET", `/apps/email-senders/${e(params.id)}`, undefined, undefined)
  }

  /** Get an email template by id */
  getEmailTemplateById<T = unknown>(params: { id: string }): Promise<T> {
    return this.req<T>("GET", `/apps/email-templates/${e(params.id)}`, undefined, undefined)
  }

  /** Get file metadata by id */
  getFileDetails<T = unknown>(params: { id: string }): Promise<T> {
    return this.req<T>("GET", `/file-details/${e(params.id)}`, undefined, undefined)
  }

  /** Get one form by id */
  getFormById<T = unknown>(params: { id: string }): Promise<T> {
    return this.req<T>("GET", `/apps/forms/${e(params.id)}`, undefined, undefined)
  }

  /** Get a use-case by id */
  getUseCase<T = unknown>(params: { id: string }): Promise<T> {
    return this.req<T>("GET", `/use-cases/${e(params.id)}`, undefined, undefined)
  }

  /** Get the use-case backing an AI assistant */
  getUseCaseByAssistant<T = unknown>(params: { assistant_id: string }): Promise<T> {
    return this.req<T>("GET", `/use-cases/by-assistant/${e(params.assistant_id)}`, undefined, undefined)
  }

  /** List app instances for the organization */
  listApps<T = unknown>(): Promise<T> {
    return this.req<T>("GET", `/apps`, undefined, undefined)
  }

  /** List email senders */
  listEmailSenders<T = unknown>(params?: { search?: string; limit?: number; skip?: number }): Promise<T> {
    return this.req<T>("GET", `/apps/email-senders`, { search: params?.search, limit: params?.limit, skip: params?.skip }, undefined)
  }

  /** List/search email templates */
  listEmailTemplates<T = unknown>(params?: { search?: string; sort?: string; limit?: number; skip?: number }): Promise<T> {
    return this.req<T>("GET", `/apps/email-templates`, { search: params?.search, sort: params?.sort, limit: params?.limit, skip: params?.skip }, undefined)
  }

  /** List forms in the organization */
  listForms<T = unknown>(params?: { search?: string; limit?: number; skip?: number }): Promise<T> {
    return this.req<T>("GET", `/apps/forms`, { search: params?.search, limit: params?.limit, skip: params?.skip }, undefined)
  }

  /** List forms tied to a data board */
  listFormsByBoard<T = unknown>(params: { boardId: string }): Promise<T> {
    return this.req<T>("GET", `/apps/forms/board/${e(params.boardId)}`, undefined, undefined)
  }

  /** List the organization's files */
  listOrganizationFiles<T = unknown>(): Promise<T> {
    return this.req<T>("GET", `/files`, undefined, undefined)
  }

  /** List organization members' emails */
  listOrgMemberEmails<T = unknown>(): Promise<T> {
    return this.req<T>("GET", `/apps/org-members-email`, undefined, undefined)
  }

  /** List channels already in use by the organization */
  listUsedChannels<T = unknown>(params: { ids: unknown[]; product_id?: string }): Promise<T> {
    return this.req<T>("GET", `/apps/used-channels`, { ids: params.ids, product_id: params.product_id }, undefined)
  }

  /** List channels in use (v2 shape) */
  listUsedChannelsV2<T = unknown>(params: { ids: unknown[] }): Promise<T> {
    return this.req<T>("GET", `/apps/used-channels-v2`, { ids: params.ids }, undefined)
  }

  /** Advanced content search of email templates */
  searchEmailTemplates<T = unknown>(params?: { field?: string; q?: string; limit?: number; skip?: number }): Promise<T> {
    return this.req<T>("GET", `/apps/email-templates/search`, { field: params?.field, q: params?.q, limit: params?.limit, skip: params?.skip }, undefined)
  }

  /** Update an email sender — @write */
  updateEmailSender<T = unknown>(params: { id: string; body: { name?: string; email?: string } }): Promise<T> {
    return this.req<T>("PUT", `/apps/email-senders/${e(params.id)}`, undefined, params.body)
  }

  /** Update an email template — @write */
  updateEmailTemplate<T = unknown>(params: { id: string; body: { name?: string; subject?: string; body?: string } }): Promise<T> {
    return this.req<T>("PUT", `/apps/email-templates/${e(params.id)}`, undefined, params.body)
  }

  /** Update file metadata — @write */
  updateFile<T = unknown>(params: { id: string; body: { name?: string; is_public?: boolean } }): Promise<T> {
    return this.req<T>("PUT", `/files/${e(params.id)}`, undefined, params.body)
  }

  /** Update a form — @write */
  updateForm<T = unknown>(params: { id: string; body: { name?: string; fields?: unknown[]; teams?: unknown[] } }): Promise<T> {
    return this.req<T>("PUT", `/apps/forms/${e(params.id)}`, undefined, params.body)
  }
}
