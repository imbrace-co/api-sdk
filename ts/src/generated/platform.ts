// AUTO-GENERATED by scripts/generate-api.mjs — DO NOT EDIT.
// Regenerate with `npm run codegen`.

import { GeneratedResource, e } from './runtime.js'

/**
 * platform — 28 operations (4 destructive).
 *
 * Generated from `specs/platform.json`.
 *
 * 0 operation(s) return a described shape; the other 28 return `unknown`
 * because their catalog declares no response schema. Every method still takes a
 * type argument, so a caller can name a shape the spec doesn't know:
 * `listBoards<{ data: Board[] }>()`.
 */
export class PlatformApi extends GeneratedResource {
  /** Add members to a team — @write */
  addTeamUsers<T = unknown>(params: { body: { team_id: string; users: unknown[] } }): Promise<T> {
    return this.req<T>("POST", `/teams/_add_users`, undefined, params.body)
  }

  /** Count users per role */
  countUsersByRole<T = unknown>(): Promise<T> {
    return this.req<T>("GET", `/users/_roles_count`, undefined, undefined)
  }

  /** Create a business unit — @write */
  createBusinessUnit<T = unknown>(params: { body: { name: string } }): Promise<T> {
    return this.req<T>("POST", `/business_units`, undefined, params.body)
  }

  /** Create a category — @write */
  createCategory<T = unknown>(params: { body: { name: string; description?: string; apply_to?: unknown[] } }): Promise<T> {
    return this.req<T>("POST", `/categories`, undefined, params.body)
  }

  /** Create a team — @write */
  createTeam<T = unknown>(params: { body: { name: string; business_unit_id?: string; description?: string; icon_url?: string } }): Promise<T> {
    return this.req<T>("POST", `/teams`, undefined, params.body)
  }

  /** ⚠️ DESTRUCTIVE — Delete a business unit — @destructive */
  deleteBusinessUnit<T = unknown>(params: { id: string }): Promise<T> {
    return this.req<T>("DELETE", `/business_units/${e(params.id)}`, undefined, undefined)
  }

  /** ⚠️ DESTRUCTIVE — Delete a category — @destructive */
  deleteCategory<T = unknown>(params: { id: string }): Promise<T> {
    return this.req<T>("DELETE", `/categories/${e(params.id)}`, undefined, undefined)
  }

  /** ⚠️ DESTRUCTIVE — Delete a team — @destructive */
  deleteTeam<T = unknown>(params: { team_id: string }): Promise<T> {
    return this.req<T>("DELETE", `/teams/${e(params.team_id)}`, undefined, undefined)
  }

  /** Get one business unit by id */
  getBusinessUnitById<T = unknown>(params: { id: string }): Promise<T> {
    return this.req<T>("GET", `/business_units/${e(params.id)}`, undefined, undefined)
  }

  /** Get one category by id */
  getCategoryById<T = unknown>(params: { id: string }): Promise<T> {
    return this.req<T>("GET", `/categories/${e(params.id)}`, undefined, undefined)
  }

  /** Get the current user */
  getCurrentUser<T = unknown>(): Promise<T> {
    return this.req<T>("GET", `/users/_me`, undefined, undefined)
  }

  /** Get the current account (roles, org, permissions, tier) */
  getMyAccount<T = unknown>(): Promise<T> {
    return this.req<T>("GET", `/account`, undefined, undefined)
  }

  /** List the caller's teams */
  getMyTeams<T = unknown>(): Promise<T> {
    return this.req<T>("GET", `/teams/my`, undefined, undefined)
  }

  /** Get one organization by id */
  getOrganizationById<T = unknown>(params: { id: string }): Promise<T> {
    return this.req<T>("GET", `/organizations/${e(params.id)}`, undefined, undefined)
  }

  /** Get a team and its members */
  getTeamById<T = unknown>(params: { team_id: string }): Promise<T> {
    return this.req<T>("GET", `/teams/${e(params.team_id)}`, undefined, undefined)
  }

  /** Get one user by id */
  getUserById<T = unknown>(params: { id: string }): Promise<T> {
    return this.req<T>("GET", `/users/${e(params.id)}`, undefined, undefined)
  }

  /** Simple list of users in the organization */
  listAllUsers<T = unknown>(): Promise<T> {
    return this.req<T>("GET", `/users/_all`, undefined, undefined)
  }

  /** List business units in the organization */
  listBusinessUnits<T = unknown>(params?: { skip?: number; limit?: number }): Promise<T> {
    return this.req<T>("GET", `/business_units`, { skip: params?.skip, limit: params?.limit }, undefined)
  }

  /** List organization categories */
  listCategories<T = unknown>(params?: { search?: string; sort?: string; apply_to?: string }): Promise<T> {
    return this.req<T>("GET", `/categories`, { search: params?.search, sort: params?.sort, apply_to: params?.apply_to }, undefined)
  }

  /** List users invitable to a team */
  listInvitableUsers<T = unknown>(params: { type: string; team_id: string }): Promise<T> {
    return this.req<T>("GET", `/team_users/_invite_list`, { type: params.type, team_id: params.team_id }, undefined)
  }

  /** List a team's labels */
  listTeamLabels<T = unknown>(params: { team_id: string; skip?: number; limit?: number }): Promise<T> {
    return this.req<T>("GET", `/teams/${e(params.team_id)}/team_labels`, { skip: params.skip, limit: params.limit }, undefined)
  }

  /** List teams (filter by business unit) */
  listTeams<T = unknown>(params: { type: string; q: string; search?: string; skip?: number; limit?: number }): Promise<T> {
    return this.req<T>("GET", `/teams`, { type: params.type, q: params.q, search: params.search, skip: params.skip, limit: params.limit }, undefined)
  }

  /** List members of a team */
  listTeamUsers<T = unknown>(params: { type: string; q: string; search?: string; skip?: number; limit?: number }): Promise<T> {
    return this.req<T>("GET", `/team_users`, { type: params.type, q: params.q, search: params.search, skip: params.skip, limit: params.limit }, undefined)
  }

  /** ⚠️ DESTRUCTIVE — Remove members from a team — @destructive */
  removeTeamUsers<T = unknown>(params: { body: { team_id: string; user_ids: unknown[] } }): Promise<T> {
    return this.req<T>("POST", `/teams/_remove_users`, undefined, params.body)
  }

  /** Update a business unit — @write */
  updateBusinessUnit<T = unknown>(params: { id: string; body: { name?: string } }): Promise<T> {
    return this.req<T>("PUT", `/business_units/${e(params.id)}`, undefined, params.body)
  }

  /** Update a category — @write */
  updateCategory<T = unknown>(params: { id: string; body: { name?: string; description?: string; apply_to?: unknown[] } }): Promise<T> {
    return this.req<T>("PUT", `/categories/${e(params.id)}`, undefined, params.body)
  }

  /** Update a team — @write */
  updateTeam<T = unknown>(params: { team_id: string; body: { name?: string; is_disabled?: boolean; icon_url?: string } }): Promise<T> {
    return this.req<T>("PUT", `/teams/${e(params.team_id)}`, undefined, params.body)
  }

  /** Update a user's profile / status / role — @write */
  updateUser<T = unknown>(params: { id: string; body: { display_name?: string; first_name?: string; last_name?: string; phone_number?: string; language?: string; status?: string; role?: string } }): Promise<T> {
    return this.req<T>("PUT", `/users/${e(params.id)}`, undefined, params.body)
  }
}
