// AUTO-GENERATED by scripts/generate-api.mjs — DO NOT EDIT.
// Regenerate with `npm run codegen`.

import { GeneratedResource, e } from './runtime.js'

/**
 * channel — 91 operations (11 destructive).
 *
 * Generated from `specs/channel.json`.
 *
 * 0 operation(s) return a described shape; the other 91 return `unknown`
 * because their catalog declares no response schema. Every method still takes a
 * type argument, so a caller can name a shape the spec doesn't know:
 * `listBoards<{ data: Board[] }>()`.
 */
export class ChannelApi extends GeneratedResource {
  /** Assign a team member to a conversation — @write */
  assignConversationMember<T = unknown>(params: { body: { conversation_id: string; user_id?: string; team_id?: string } }): Promise<T> {
    return this.req<T>("POST", `/team_conversations/assign_team_member`, undefined, params.body)
  }

  /** Activate/deactivate multiple channels at once — @write */
  bulkUpdateChannelStatus<T = unknown>(params: { body: { channel_ids?: unknown[]; status: string } }): Promise<T> {
    return this.req<T>("PATCH", `/channels/status`, undefined, params.body)
  }

  /** Count channels grouped by type */
  countChannelsByType<T = unknown>(): Promise<T> {
    return this.req<T>("GET", `/channels/_count`, undefined, undefined)
  }

  /** Count the current user's conversations per channel type */
  countConversationsPerChannel<T = unknown>(params?: { view?: string; team_id?: string; status?: string }): Promise<T> {
    return this.req<T>("GET", `/channels/_conv_count`, { view: params?.view, team_id: params?.team_id, status: params?.status }, undefined)
  }

  /** Count conversations per view (all/joined/yours) */
  countConversationViews<T = unknown>(): Promise<T> {
    return this.req<T>("GET", `/team_conversations/_views_count`, undefined, undefined)
  }

  /** Create a campaign — @write */
  createCampaign<T = unknown>(params: { body: { name: string } }): Promise<T> {
    return this.req<T>("POST", `/campaign`, undefined, params.body)
  }

  /** Create a contact — @write */
  createContact<T = unknown>(params: { body: { display_name?: string; first_name?: string; last_name?: string; email?: string; phone_number?: string; channel_id?: string; channel_type?: string; labels?: unknown[]; time_zone?: string } }): Promise<T> {
    return this.req<T>("POST", `/contacts`, undefined, params.body)
  }

  /** Add a comment (note) to a contact — @write */
  createContactComment<T = unknown>(params: { id: string; body: { content: string } }): Promise<T> {
    return this.req<T>("POST", `/contacts/${e(params.id)}/comments`, undefined, params.body)
  }

  /** Create an Email channel — @write */
  createEmailChannel<T = unknown>(params: { body: { name: string; config?: Record<string, unknown> } }): Promise<T> {
    return this.req<T>("POST", `/channels/_email`, undefined, params.body)
  }

  /** Create a Facebook Messenger channel — @write */
  createFacebookChannel<T = unknown>(params: { body: { name: string; config?: Record<string, unknown> } }): Promise<T> {
    return this.req<T>("POST", `/channels/_facebook`, undefined, params.body)
  }

  /** Create an Instagram channel — @write */
  createInstagramChannel<T = unknown>(params: { body: { name: string; config?: Record<string, unknown> } }): Promise<T> {
    return this.req<T>("POST", `/channels/_instagram`, undefined, params.body)
  }

  /** Create a LINE channel — @write */
  createLineChannel<T = unknown>(params: { body: { name: string; config?: Record<string, unknown> } }): Promise<T> {
    return this.req<T>("POST", `/channels/_line`, undefined, params.body)
  }

  /** Comment on a message (internal note) — @write */
  createMessageComment<T = unknown>(params: { conversation_id: string; message_id: string; body: { content: string } }): Promise<T> {
    return this.req<T>("POST", `/conversations/${e(params.conversation_id)}/conversation_messages/${e(params.message_id)}/comments`, undefined, params.body)
  }

  /** Create a message template — @write */
  createMessageTemplate<T = unknown>(params: { body: { title: string; text: string; category?: string; template_language?: string } }): Promise<T> {
    return this.req<T>("POST", `/message_templates`, undefined, params.body)
  }

  /** Create a touchpoint (routing rule / entry point) — @write */
  createTouchpoint<T = unknown>(params: { body: { name: string; channel_id?: string; initial_phrase?: string; execute_workflow_id?: string; url?: string } }): Promise<T> {
    return this.req<T>("POST", `/touchpoints`, undefined, params.body)
  }

  /** Create a Web (widget) channel — @write */
  createWebChannel<T = unknown>(params: { body: { name: string; config?: Record<string, unknown> } }): Promise<T> {
    return this.req<T>("POST", `/channels/_web`, undefined, params.body)
  }

  /** Create a WeChat channel — @write */
  createWechatChannel<T = unknown>(params: { body: { name: string; config?: Record<string, unknown> } }): Promise<T> {
    return this.req<T>("POST", `/channels/_wechat`, undefined, params.body)
  }

  /** Create a WhatsApp channel — @write */
  createWhatsappChannel<T = unknown>(params: { body: { name: string; config?: Record<string, unknown> } }): Promise<T> {
    return this.req<T>("POST", `/channels/_whatsapp`, undefined, params.body)
  }

  /** ⚠️ DESTRUCTIVE — Delete a campaign — @destructive */
  deleteCampaign<T = unknown>(params: { id: string }): Promise<T> {
    return this.req<T>("DELETE", `/campaign/${e(params.id)}`, undefined, undefined)
  }

  /** ⚠️ DESTRUCTIVE — Delete a channel — @destructive */
  deleteChannel<T = unknown>(params: { id: string }): Promise<T> {
    return this.req<T>("DELETE", `/channels/${e(params.id)}`, undefined, undefined)
  }

  /** ⚠️ DESTRUCTIVE — Delete a stored channel credential — @destructive */
  deleteChannelCredential<T = unknown>(params: { credential_id: string }): Promise<T> {
    return this.req<T>("DELETE", `/channels/_facebook/credential/${e(params.credential_id)}`, undefined, undefined)
  }

  /** ⚠️ DESTRUCTIVE — Delete a contact — @destructive */
  deleteContact<T = unknown>(params: { id: string }): Promise<T> {
    return this.req<T>("DELETE", `/contacts/${e(params.id)}`, undefined, undefined)
  }

  /** ⚠️ DESTRUCTIVE — Delete a contact comment — @destructive */
  deleteContactComment<T = unknown>(params: { id: string; commentId: string }): Promise<T> {
    return this.req<T>("DELETE", `/contacts/${e(params.id)}/comment/${e(params.commentId)}`, undefined, undefined)
  }

  /** ⚠️ DESTRUCTIVE — Delete a conversation — @destructive */
  deleteConversation<T = unknown>(params: { id: string }): Promise<T> {
    return this.req<T>("DELETE", `/team_conversations/${e(params.id)}`, undefined, undefined)
  }

  /** ⚠️ DESTRUCTIVE — Delete a conversation comment — @destructive */
  deleteConversationComment<T = unknown>(params: { conversation_id: string; comment_id: string }): Promise<T> {
    return this.req<T>("DELETE", `/conversations/${e(params.conversation_id)}/comments/${e(params.comment_id)}`, undefined, undefined)
  }

  /** ⚠️ DESTRUCTIVE — Delete a message template — @destructive */
  deleteMessageTemplate<T = unknown>(params: { id: string }): Promise<T> {
    return this.req<T>("DELETE", `/message_templates/${e(params.id)}`, undefined, undefined)
  }

  /** ⚠️ DESTRUCTIVE — Delete a touchpoint — @destructive */
  deleteTouchpoint<T = unknown>(params: { id: string }): Promise<T> {
    return this.req<T>("DELETE", `/touchpoints/${e(params.id)}`, undefined, undefined)
  }

  /** Dismiss all notifications — @write */
  dismissAllNotifications<T = unknown>(): Promise<T> {
    return this.req<T>("POST", `/notifications/dismiss/all`, undefined, undefined)
  }

  /** Dismiss notifications — @write */
  dismissNotifications<T = unknown>(params: { body: { ids?: unknown[] } }): Promise<T> {
    return this.req<T>("POST", `/notifications/dismiss`, undefined, params.body)
  }

  /** Get a campaign by id */
  getCampaign<T = unknown>(params: { id: string }): Promise<T> {
    return this.req<T>("GET", `/campaign/${e(params.id)}`, undefined, undefined)
  }

  /** Get a single channel by id */
  getChannelById<T = unknown>(params: { id: string }): Promise<T> {
    return this.req<T>("GET", `/channels/${e(params.id)}`, undefined, undefined)
  }

  /** Get a stored channel credential */
  getChannelCredential<T = unknown>(params: { credential_id: string }): Promise<T> {
    return this.req<T>("GET", `/channels/_facebook/credential/${e(params.credential_id)}`, undefined, undefined)
  }

  /** Get the workflow config attached to a channel */
  getChannelWorkflow<T = unknown>(params: { id: string }): Promise<T> {
    return this.req<T>("GET", `/channels/workflows/${e(params.id)}`, undefined, undefined)
  }

  /** Get a contact by id */
  getContact<T = unknown>(params: { id: string }): Promise<T> {
    return this.req<T>("GET", `/contacts/${e(params.id)}`, undefined, undefined)
  }

  /** Resolve a contact from a databoard item id */
  getContactByBoardItem<T = unknown>(params: { boardItemId: string }): Promise<T> {
    return this.req<T>("GET", `/contacts/by-board-item/${e(params.boardItemId)}`, undefined, undefined)
  }

  /** Get one contact comment */
  getContactComment<T = unknown>(params: { id: string; commentId: string }): Promise<T> {
    return this.req<T>("GET", `/contacts/${e(params.id)}/comment/${e(params.commentId)}`, undefined, undefined)
  }

  /** Get one conversation with details */
  getConversation<T = unknown>(params: { id: string }): Promise<T> {
    return this.req<T>("GET", `/team_conversations/${e(params.id)}`, undefined, undefined)
  }

  /** Get a single message */
  getConversationMessage<T = unknown>(params: { conversation_id: string; message_id: string }): Promise<T> {
    return this.req<T>("GET", `/conversations/${e(params.conversation_id)}/conversation_messages/${e(params.message_id)}`, undefined, undefined)
  }

  /** Get an external data-sync subscription */
  getExternalDataSync<T = unknown>(params: { id: string }): Promise<T> {
    return this.req<T>("GET", `/external-data-sync/${e(params.id)}`, undefined, undefined)
  }

  /** Get the position (index) of a message inside its conversation */
  getMessageIndex<T = unknown>(params: { conversation_id: string; conversation_message_id: string }): Promise<T> {
    return this.req<T>("GET", `/conversations/${e(params.conversation_id)}/conversation_messages/${e(params.conversation_message_id)}/_index`, undefined, undefined)
  }

  /** Get a message template */
  getMessageTemplate<T = unknown>(params: { id: string }): Promise<T> {
    return this.req<T>("GET", `/message_templates/${e(params.id)}`, undefined, undefined)
  }

  /** Get a touchpoint by id */
  getTouchpoint<T = unknown>(params: { id: string }): Promise<T> {
    return this.req<T>("GET", `/touchpoints/${e(params.id)}`, undefined, undefined)
  }

  /** Get or create the organization's default web channel — @write */
  initDefaultWebChannel<T = unknown>(params: { body: { name?: string } }): Promise<T> {
    return this.req<T>("POST", `/init_channel`, undefined, params.body)
  }

  /** Join a conversation as team member — @write */
  joinConversation<T = unknown>(params: { body: { conversation_id: string } }): Promise<T> {
    return this.req<T>("POST", `/team_conversations/_join`, undefined, params.body)
  }

  /** Leave a conversation — @write */
  leaveConversation<T = unknown>(params: { body: { conversation_id: string } }): Promise<T> {
    return this.req<T>("POST", `/team_conversations/_leave`, undefined, params.body)
  }

  /** List teams available for conversation assignment */
  listAssignableTeams<T = unknown>(params: { conversation_id: string }): Promise<T> {
    return this.req<T>("GET", `/assign/teams/all`, { conversation_id: params.conversation_id }, undefined)
  }

  /** List campaigns */
  listCampaigns<T = unknown>(): Promise<T> {
    return this.req<T>("GET", `/campaign`, undefined, undefined)
  }

  /** List channel-automation workflows available to the org */
  listChannelAutomationWorkflows<T = unknown>(): Promise<T> {
    return this.req<T>("GET", `/workflows/channel_automation`, undefined, undefined)
  }

  /** List supported channel providers */
  listChannelProviders<T = unknown>(): Promise<T> {
    return this.req<T>("GET", `/channels/providers`, undefined, undefined)
  }

  /** List communication channels of the organization */
  listChannels<T = unknown>(params?: { type?: string; limit?: number; offset?: number }): Promise<T> {
    return this.req<T>("GET", `/channels`, { type: params?.type, limit: params?.limit, offset: params?.offset }, undefined)
  }

  /** List comments (notes) on a contact */
  listContactComments<T = unknown>(params: { id: string }): Promise<T> {
    return this.req<T>("GET", `/contacts/${e(params.id)}/comments`, undefined, undefined)
  }

  /** List conversations of a contact */
  listContactConversations<T = unknown>(params: { id: string }): Promise<T> {
    return this.req<T>("GET", `/contacts/${e(params.id)}/conversations`, undefined, undefined)
  }

  /** List files exchanged with a contact */
  listContactFiles<T = unknown>(params: { id: string }): Promise<T> {
    return this.req<T>("GET", `/contacts/${e(params.id)}/files`, undefined, undefined)
  }

  /** List message comments involving a contact */
  listContactMessageComments<T = unknown>(params: { id: string }): Promise<T> {
    return this.req<T>("GET", `/contacts/${e(params.id)}/message_comments`, undefined, undefined)
  }

  /** List contacts of the organization */
  listContacts<T = unknown>(params?: { q?: string; limit?: number; offset?: number }): Promise<T> {
    return this.req<T>("GET", `/contacts`, { q: params?.q, limit: params?.limit, offset: params?.offset }, undefined)
  }

  /** List activity log entries of a conversation */
  listConversationActivities<T = unknown>(params: { conversationId: string }): Promise<T> {
    return this.req<T>("GET", `/conversations_activities/${e(params.conversationId)}`, undefined, undefined)
  }

  /** List messages of a conversation */
  listConversationMessages<T = unknown>(params: { conversation_id: string; limit?: number; skip?: number }): Promise<T> {
    return this.req<T>("GET", `/conversation_messages`, { conversation_id: params.conversation_id, limit: params.limit, skip: params.skip }, undefined)
  }

  /** List team conversations (inbox) */
  listConversations<T = unknown>(params?: { status?: string; channel_id?: string; team_id?: string; contact_id?: string; view?: string; search?: string; limit?: number; skip?: number }): Promise<T> {
    return this.req<T>("GET", `/team_conversations`, { status: params?.status, channel_id: params?.channel_id, team_id: params?.team_id, contact_id: params?.contact_id, view: params?.view, search: params?.search, limit: params?.limit, skip: params?.skip }, undefined)
  }

  /** List users participating in a conversation */
  listConversationUsers<T = unknown>(params: { id: string }): Promise<T> {
    return this.req<T>("GET", `/team_conversations/${e(params.id)}/users`, undefined, undefined)
  }

  /** List processed credential types (static catalog) */
  listCredentialTypes<T = unknown>(): Promise<T> {
    return this.req<T>("GET", `/workflow/processed-credential-types`, undefined, undefined)
  }

  /** List external data-sync subscriptions */
  listExternalDataSyncs<T = unknown>(): Promise<T> {
    return this.req<T>("GET", `/external-data-sync`, undefined, undefined)
  }

  /** List reusable message templates */
  listMessageTemplates<T = unknown>(params?: { limit?: number; offset?: number }): Promise<T> {
    return this.req<T>("GET", `/message_templates`, { limit: params?.limit, offset: params?.offset }, undefined)
  }

  /** List in-app notifications of the current user */
  listNotifications<T = unknown>(params?: { type?: string; limit?: number; skip?: number }): Promise<T> {
    return this.req<T>("GET", `/notifications`, { type: params?.type, limit: params?.limit, skip: params?.skip }, undefined)
  }

  /** List conversations awaiting a reply */
  listOutstandingConversations<T = unknown>(): Promise<T> {
    return this.req<T>("GET", `/team_conversations/_outstanding`, undefined, undefined)
  }

  /** List observer users of a team */
  listTeamObservers<T = unknown>(params: { teamId: string }): Promise<T> {
    return this.req<T>("GET", `/assign/team/${e(params.teamId)}/observers`, undefined, undefined)
  }

  /** List routing touchpoints */
  listTouchpoints<T = unknown>(params?: { status?: string; channel_id?: string; campaign_id?: string; is_archived?: boolean; limit?: number; offset?: number }): Promise<T> {
    return this.req<T>("GET", `/touchpoints`, { status: params?.status, channel_id: params?.channel_id, campaign_id: params?.campaign_id, is_archived: params?.is_archived, limit: params?.limit, offset: params?.offset }, undefined)
  }

  /** List WhatsApp HSM templates */
  listWhatsappTemplates<T = unknown>(): Promise<T> {
    return this.req<T>("GET", `/whatsapp_templates`, undefined, undefined)
  }

  /** List workflow credential catalog (static) */
  listWorkflowCredentials<T = unknown>(): Promise<T> {
    return this.req<T>("GET", `/credentials`, undefined, undefined)
  }

  /** Mark notifications as read — @write */
  markNotificationsRead<T = unknown>(params: { body: { ids?: unknown[] } }): Promise<T> {
    return this.req<T>("PUT", `/notifications/read`, undefined, params.body)
  }

  /** Peek the workflow stack of a conversation */
  peekWorkflowStack<T = unknown>(params: { conversation_id: string }): Promise<T> {
    return this.req<T>("GET", `/stacks/${e(params.conversation_id)}`, undefined, undefined)
  }

  /** Remove a team member from a conversation — @write */
  removeConversationMember<T = unknown>(params: { body: { conversation_id: string; user_id: string } }): Promise<T> {
    return this.req<T>("POST", `/team_conversations/remove_team_member`, undefined, params.body)
  }

  /** Rename a conversation — @write */
  renameConversation<T = unknown>(params: { body: { conversation_id: string; name: string } }): Promise<T> {
    return this.req<T>("POST", `/team_conversations/_update_name`, undefined, params.body)
  }

  /** Request to join a conversation — @write */
  requestJoinConversation<T = unknown>(params: { body: { conversation_id: string } }): Promise<T> {
    return this.req<T>("POST", `/team_conversations/_join_request`, undefined, params.body)
  }

  /** ⚠️ DESTRUCTIVE — Reset the workflow position of a conversation (unstick it) — @destructive */
  resetConversationWorkflow<T = unknown>(params: { id: string }): Promise<T> {
    return this.req<T>("DELETE", `/positions/${e(params.id)}`, undefined, undefined)
  }

  /** ⚠️ DESTRUCTIVE — Reset workflow positions for every conversation on a workflow — @destructive */
  resetWorkflowPositions<T = unknown>(params: { id: string }): Promise<T> {
    return this.req<T>("DELETE", `/positions/workflow/${e(params.id)}`, undefined, undefined)
  }

  /** Search contacts — @write */
  searchContacts<T = unknown>(params: { body: { query: string; limit?: number; offset?: number } }): Promise<T> {
    return this.req<T>("POST", `/contacts/_search`, undefined, params.body)
  }

  /** Search conversations by text */
  searchConversations<T = unknown>(params: { q: string; limit?: number; skip?: number }): Promise<T> {
    return this.req<T>("GET", `/team_conversations/_search`, { q: params.q, limit: params.limit, skip: params.skip }, undefined)
  }

  /** Search message templates */
  searchMessageTemplates<T = unknown>(params: { q: string }): Promise<T> {
    return this.req<T>("GET", `/message_templates/_search`, { q: params.q }, undefined)
  }

  /** Send a message into a conversation — @write */
  sendConversationMessage<T = unknown>(params: { body: { conversation_id: string; type?: string; text?: string; content?: Record<string, unknown>; url?: string; caption?: string } }): Promise<T> {
    return this.req<T>("POST", `/conversation_messages`, undefined, params.body)
  }

  /** Trigger a manual re-sync — @write */
  syncExternalDataSync<T = unknown>(params: { id: string; body: { time_frame_start?: string; time_frame_end?: string } }): Promise<T> {
    return this.req<T>("POST", `/external-data-sync/${e(params.id)}/sync`, undefined, params.body)
  }

  /** Update a campaign — @write */
  updateCampaign<T = unknown>(params: { id: string; body: { name: string } }): Promise<T> {
    return this.req<T>("PUT", `/campaign/${e(params.id)}`, undefined, params.body)
  }

  /** Update a channel (name, config, active flag) — @write */
  updateChannel<T = unknown>(params: { id: string; body: { name?: string; config?: Record<string, unknown>; active?: boolean } }): Promise<T> {
    return this.req<T>("PUT", `/channels/${e(params.id)}`, undefined, params.body)
  }

  /** Update a stored channel credential — @write */
  updateChannelCredential<T = unknown>(params: { credential_id: string; body: { credentials?: Record<string, unknown> } }): Promise<T> {
    return this.req<T>("PUT", `/channels/_facebook/credential/${e(params.credential_id)}`, undefined, params.body)
  }

  /** Update contact fields — @write */
  updateContact<T = unknown>(params: { id: string; body: { display_name?: string; email?: string; phone_number?: string; labels?: unknown[] } }): Promise<T> {
    return this.req<T>("PUT", `/contacts/${e(params.id)}`, undefined, params.body)
  }

  /** Update conversation fields — @write */
  updateConversation<T = unknown>(params: { id: string; body: { name?: string; status?: string; assigned_to?: string } }): Promise<T> {
    return this.req<T>("PUT", `/team_conversations/${e(params.id)}`, undefined, params.body)
  }

  /** Edit a conversation comment — @write */
  updateConversationComment<T = unknown>(params: { conversation_id: string; comment_id: string; body: { content: string } }): Promise<T> {
    return this.req<T>("PUT", `/conversations/${e(params.conversation_id)}/comments/${e(params.comment_id)}`, undefined, params.body)
  }

  /** Update a message — @write */
  updateConversationMessage<T = unknown>(params: { conversation_id: string; message_id: string; body: { text?: string; labels?: unknown[] } }): Promise<T> {
    return this.req<T>("PUT", `/conversations/${e(params.conversation_id)}/conversation_messages/${e(params.message_id)}`, undefined, params.body)
  }

  /** Change conversation status (open/resolved/archived/…) — @write */
  updateConversationStatus<T = unknown>(params: { body: { conversation_id: string; status: string } }): Promise<T> {
    return this.req<T>("POST", `/team_conversations/_update_status`, undefined, params.body)
  }

  /** Update a message template — @write */
  updateMessageTemplate<T = unknown>(params: { id: string; body: { title?: string; text?: string } }): Promise<T> {
    return this.req<T>("PUT", `/message_templates/${e(params.id)}`, undefined, params.body)
  }

  /** Update a touchpoint — @write */
  updateTouchpoint<T = unknown>(params: { id: string; body: { name?: string; initial_phrase?: string; status?: string } }): Promise<T> {
    return this.req<T>("PUT", `/touchpoints/${e(params.id)}`, undefined, params.body)
  }

  /** Validate a touchpoint config (e.g. duplicate initial phrase) — @write */
  validateTouchpoint<T = unknown>(params: { body: { channel_id?: string; initial_phrase?: string } }): Promise<T> {
    return this.req<T>("POST", `/touchpoints/_validate`, undefined, params.body)
  }
}
