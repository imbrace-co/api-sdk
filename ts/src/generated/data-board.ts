// AUTO-GENERATED by scripts/generate-api.mjs — DO NOT EDIT.
// Regenerate with `npm run codegen`.

import { GeneratedResource, e } from './runtime.js'

/**
 * data-board — 79 operations (16 destructive).
 *
 * Generated from `specs/data-board.json`.
 *
 * 0 operation(s) return a described shape; the other 79 return `unknown`
 * because their catalog declares no response schema. Every method still takes a
 * type argument, so a caller can name a shape the spec doesn't know:
 * `listBoards<{ data: Board[] }>()`.
 */
export class DataBoardApi extends GeneratedResource {
  /** Add a field to a board — @write */
  addBoardField<T = unknown>(params: { id: string; body: { name: string; type: string } }): Promise<T> {
    return this.req<T>("POST", `/boards/${e(params.id)}/fields`, undefined, params.body)
  }

  /** Run a read-only filtered query/aggregation over a board's items */
  aggregateBoardItems<T = unknown>(params: { boardId: string; body: { query?: Record<string, unknown> } }): Promise<T> {
    return this.req<T>("POST", `/boards/${e(params.boardId)}/aggregate`, undefined, params.body)
  }

  /** Create many items at once — @write */
  bulkCreateBoardItems<T = unknown>(params: { boardId: string; body: { items: unknown[] } }): Promise<T> {
    return this.req<T>("POST", `/boards/${e(params.boardId)}/items/bulk`, undefined, params.body)
  }

  /** ⚠️ DESTRUCTIVE — Delete many items by id list — @destructive */
  bulkDeleteBoardItems<T = unknown>(params: { boardId: string; body: { ids: unknown[] } }): Promise<T> {
    return this.req<T>("DELETE", `/boards/${e(params.boardId)}/items/bulk-delete`, undefined, params.body)
  }

  /** ⚠️ DESTRUCTIVE — Update many items by id list (mass mutation) — @destructive */
  bulkUpdateBoardItems<T = unknown>(params: { boardId: string; body: { ids: unknown[]; updates: Record<string, unknown> } }): Promise<T> {
    return this.req<T>("PATCH", `/boards/${e(params.boardId)}/items/bulk-update`, undefined, params.body)
  }

  /** ⚠️ DESTRUCTIVE — Bulk create/update fields — can restructure the board schema — @destructive */
  bulkUpsertBoardFields<T = unknown>(params: { boardId: string; body: { fields: unknown[] } }): Promise<T> {
    return this.req<T>("PATCH", `/boards/${e(params.boardId)}/fields/bulk`, undefined, params.body)
  }

  /** Count items in a board */
  countBoardItems<T = unknown>(params: { boardId: string; filter?: string }): Promise<T> {
    return this.req<T>("GET", `/boards/${e(params.boardId)}/items/count`, { filter: params.filter }, undefined)
  }

  /** Create a board — @write */
  createBoard<T = unknown>(params: { body: { name: string; type?: string; description?: string; category?: string } }): Promise<T> {
    return this.req<T>("POST", `/boards`, undefined, params.body)
  }

  /** Create an item in a board — @write */
  createBoardItem<T = unknown>(params: { boardId: string; body: { fields: Record<string, unknown> } }): Promise<T> {
    return this.req<T>("POST", `/boards/${e(params.boardId)}/items`, undefined, params.body)
  }

  /** Create a CRMBoard automation — @write */
  createCrmboard<T = unknown>(params: { body: { type: string; board_id: string } }): Promise<T> {
    return this.req<T>("POST", `/v1/crmboard`, undefined, params.body)
  }

  /** Create a document category — @write */
  createDocCategory<T = unknown>(params: { body: { name: string; type: string; parentId?: string } }): Promise<T> {
    return this.req<T>("POST", `/categories`, undefined, params.body)
  }

  /** Create a document schema — @write */
  createDocSchema<T = unknown>(params: { body: { name: string; description?: string; category?: string; attributes?: unknown[] } }): Promise<T> {
    return this.req<T>("POST", `/schemas`, undefined, params.body)
  }

  /** Create a folder — @write */
  createFolder<T = unknown>(params: { body: { name: string; parent_folder_id?: string; team_ids?: unknown[] } }): Promise<T> {
    return this.req<T>("POST", `/folders`, undefined, params.body)
  }

  /** Create a segmentation — @write */
  createSegmentation<T = unknown>(params: { boardId: string; body: { name?: string; description?: string; filters?: unknown[] } }): Promise<T> {
    return this.req<T>("POST", `/boards/${e(params.boardId)}/segmentation`, undefined, params.body)
  }

  /** ⚠️ DESTRUCTIVE — Delete a board and its items — @destructive */
  deleteBoard<T = unknown>(params: { id: string }): Promise<T> {
    return this.req<T>("DELETE", `/boards/${e(params.id)}`, undefined, undefined)
  }

  /** ⚠️ DESTRUCTIVE — Delete a field from a board — @destructive */
  deleteBoardField<T = unknown>(params: { id: string; fieldId: string }): Promise<T> {
    return this.req<T>("DELETE", `/boards/${e(params.id)}/fields/${e(params.fieldId)}`, undefined, undefined)
  }

  /** ⚠️ DESTRUCTIVE — Delete one item — @destructive */
  deleteBoardItem<T = unknown>(params: { boardId: string; id: string }): Promise<T> {
    return this.req<T>("DELETE", `/boards/${e(params.boardId)}/items/${e(params.id)}`, undefined, undefined)
  }

  /** ⚠️ DESTRUCTIVE — Delete every item matching a filter — @destructive */
  deleteBoardItemsByFilter<T = unknown>(params: { boardId: string; body: { filter: Record<string, unknown> } }): Promise<T> {
    return this.req<T>("DELETE", `/boards/${e(params.boardId)}/items/delete-by-filter`, undefined, params.body)
  }

  /** ⚠️ DESTRUCTIVE — Delete a CRMBoard automation — @destructive */
  deleteCrmboard<T = unknown>(params: { id: string }): Promise<T> {
    return this.req<T>("DELETE", `/v1/crmboard/board/${e(params.id)}`, undefined, undefined)
  }

  /** ⚠️ DESTRUCTIVE — Delete all CRMBoard automations for a board id — @destructive */
  deleteCrmboardsByBoard<T = unknown>(params: { boardid: string }): Promise<T> {
    return this.req<T>("DELETE", `/v1/crmboard/${e(params.boardid)}`, undefined, undefined)
  }

  /** ⚠️ DESTRUCTIVE — Delete a document category (children are promoted) — @destructive */
  deleteDocCategory<T = unknown>(params: { id: string }): Promise<T> {
    return this.req<T>("DELETE", `/categories/${e(params.id)}`, undefined, undefined)
  }

  /** ⚠️ DESTRUCTIVE — Delete a document schema — @destructive */
  deleteDocSchema<T = unknown>(params: { id: string }): Promise<T> {
    return this.req<T>("DELETE", `/schemas/${e(params.id)}`, undefined, undefined)
  }

  /** ⚠️ DESTRUCTIVE — Delete one attribute of a document schema — @destructive */
  deleteDocSchemaAttribute<T = unknown>(params: { schemaId: string; attributeId: string }): Promise<T> {
    return this.req<T>("DELETE", `/schemas/${e(params.schemaId)}/attributes/${e(params.attributeId)}`, undefined, undefined)
  }

  /** ⚠️ DESTRUCTIVE — Delete folders by id list — @destructive */
  deleteFolders<T = unknown>(params: { body: { ids: unknown[] } }): Promise<T> {
    return this.req<T>("POST", `/folders/delete`, undefined, params.body)
  }

  /** ⚠️ DESTRUCTIVE — Delete a segmentation — @destructive */
  deleteSegmentation<T = unknown>(params: { boardId: string; id: string }): Promise<T> {
    return this.req<T>("DELETE", `/boards/${e(params.boardId)}/segmentation/${e(params.id)}`, undefined, undefined)
  }

  /** Filtered list of a board's items without text search */
  fetchBoardItems<T = unknown>(params: { boardId: string; body: { limit?: number; offset?: number; filter?: string } }): Promise<T> {
    return this.req<T>("POST", `/search/${e(params.boardId)}/fetch`, undefined, params.body)
  }

  /** Get one board by id */
  getBoardById<T = unknown>(params: { id: string }): Promise<T> {
    return this.req<T>("GET", `/boards/${e(params.id)}`, undefined, undefined)
  }

  /** List boards connected to a board */
  getBoardConnections<T = unknown>(params: { boardId: string; max_depth?: number }): Promise<T> {
    return this.req<T>("GET", `/boards/${e(params.boardId)}/connections`, { max_depth: params.max_depth }, undefined)
  }

  /** Get one field definition */
  getBoardField<T = unknown>(params: { id: string; fieldId: string }): Promise<T> {
    return this.req<T>("GET", `/boards/${e(params.id)}/fields/${e(params.fieldId)}`, undefined, undefined)
  }

  /** Get the item linked to a contact */
  getBoardItemByContact<T = unknown>(params: { boardId: string; contactId: string }): Promise<T> {
    return this.req<T>("GET", `/boards/${e(params.boardId)}/items/by-contact/${e(params.contactId)}`, undefined, undefined)
  }

  /** Get one item by id */
  getBoardItemById<T = unknown>(params: { boardId: string; id: string }): Promise<T> {
    return this.req<T>("GET", `/boards/${e(params.boardId)}/items/${e(params.id)}`, undefined, undefined)
  }

  /** Get item details in the legacy name-keyed shape */
  getBoardItemDetails<T = unknown>(params: { boardId: string; id: string }): Promise<T> {
    return this.req<T>("GET", `/boards/${e(params.boardId)}/items/${e(params.id)}/details`, undefined, undefined)
  }

  /** Item tree with related/child items resolved */
  getBoardItemsWithRelations<T = unknown>(params: { boardId: string; depth?: number; limit?: number; offset?: number; item_id?: string; format?: string }): Promise<T> {
    return this.req<T>("GET", `/boards/${e(params.boardId)}/items-with-relations`, { depth: params.depth, limit: params.limit, offset: params.offset, item_id: params.item_id, format: params.format }, undefined)
  }

  /** Board model schema and per-field-type metadata */
  getBoardModelSchema<T = unknown>(): Promise<T> {
    return this.req<T>("GET", `/boards/model-schema`, undefined, undefined)
  }

  /** Self-describing RDF schema of a board for LLM agents */
  getBoardRdfSchema<T = unknown>(params: { boardId: string; depth?: number; format?: string }): Promise<T> {
    return this.req<T>("GET", `/boards/${e(params.boardId)}/rdf-schema`, { depth: params.depth, format: params.format }, undefined)
  }

  /** Get the board item for a contact */
  getBoardsByContact<T = unknown>(params: { contactId: string }): Promise<T> {
    return this.req<T>("GET", `/boards/by-contact/${e(params.contactId)}`, undefined, undefined)
  }

  /** Board schema plus a sample item */
  getBoardSchema<T = unknown>(params: { boardId: string }): Promise<T> {
    return this.req<T>("GET", `/boards/${e(params.boardId)}/schema`, undefined, undefined)
  }

  /** Get a CRMBoard automation by id */
  getCrmboardById<T = unknown>(params: { id: string }): Promise<T> {
    return this.req<T>("GET", `/v1/crmboard/board/${e(params.id)}`, undefined, undefined)
  }

  /** List CRMBoard automations referencing a field id */
  getCrmboardsByField<T = unknown>(params: { id: string }): Promise<T> {
    return this.req<T>("GET", `/v1/crmboard/field/${e(params.id)}`, undefined, undefined)
  }

  /** List CRMBoard automations bound to a workflow id */
  getCrmboardsByWorkflow<T = unknown>(params: { id: string }): Promise<T> {
    return this.req<T>("GET", `/v1/crmboard/workflow/${e(params.id)}`, undefined, undefined)
  }

  /** Get the category subtree rooted at an id */
  getDocCategorySubtree<T = unknown>(params: { id: string }): Promise<T> {
    return this.req<T>("GET", `/categories/${e(params.id)}`, undefined, undefined)
  }

  /** Get a document schema by id */
  getDocSchemaById<T = unknown>(params: { id: string }): Promise<T> {
    return this.req<T>("GET", `/schemas/${e(params.id)}`, undefined, undefined)
  }

  /** Get one version snapshot of a document schema */
  getDocSchemaVersion<T = unknown>(params: { schemaId: string; version: string }): Promise<T> {
    return this.req<T>("GET", `/schemas/${e(params.schemaId)}/versions/${e(params.version)}`, undefined, undefined)
  }

  /** Get a document file by id */
  getFile<T = unknown>(params: { id: string }): Promise<T> {
    return this.req<T>("GET", `/files/${e(params.id)}`, undefined, undefined)
  }

  /** Get a folder by id */
  getFolderById<T = unknown>(params: { id: string; recursive?: boolean }): Promise<T> {
    return this.req<T>("GET", `/folders/${e(params.id)}`, { recursive: params.recursive }, undefined)
  }

  /** List a folder's files and subfolders */
  getFolderContents<T = unknown>(params: { id: string; skip?: number; limit?: number; sortBy?: string; sortOrder?: string; recursive?: boolean; q?: string }): Promise<T> {
    return this.req<T>("GET", `/folders/${e(params.id)}/contents`, { skip: params.skip, limit: params.limit, sortBy: params.sortBy, sortOrder: params.sortOrder, recursive: params.recursive, q: params.q }, undefined)
  }

  /** Derived property-graph (ontology) view across boards */
  getOntology<T = unknown>(): Promise<T> {
    return this.req<T>("GET", `/ontology`, undefined, undefined)
  }

  /** Get one segmentation */
  getSegmentation<T = unknown>(params: { boardId: string; id: string }): Promise<T> {
    return this.req<T>("GET", `/boards/${e(params.boardId)}/segmentation/${e(params.id)}`, undefined, undefined)
  }

  /** Link related items from another board — @write */
  linkRelatedItems<T = unknown>(params: { boardId: string; id: string; body: { relatedBoardId: string; relatedItemIds: unknown[] } }): Promise<T> {
    return this.req<T>("POST", `/boards/${e(params.boardId)}/items/${e(params.id)}/related`, undefined, params.body)
  }

  /** List items in a board (paginated, filterable) */
  listBoardItems<T = unknown>(params: { boardId: string; filter?: string; sort?: string; skip?: number; limit?: number; include_parent?: boolean }): Promise<T> {
    return this.req<T>("GET", `/boards/${e(params.boardId)}/items`, { filter: params.filter, sort: params.sort, skip: params.skip, limit: params.limit, include_parent: params.include_parent }, undefined)
  }

  /** List boards in the organization (paginated, filterable) */
  listBoards<T = unknown>(params?: { search?: string; hidden?: boolean; category_id?: string; limit?: number; skip?: number }): Promise<T> {
    return this.req<T>("GET", `/boards`, { search: params?.search, hidden: params?.hidden, category_id: params?.category_id, limit: params?.limit, skip: params?.skip }, undefined)
  }

  /** List CRMBoard automations for a board id */
  listCrmboardsByBoard<T = unknown>(params: { boardid: string }): Promise<T> {
    return this.req<T>("GET", `/v1/crmboard/${e(params.boardid)}`, undefined, undefined)
  }

  /** List document categories as a tree */
  listDocCategories<T = unknown>(params?: { search?: string; type?: string; limit?: number; skip?: number }): Promise<T> {
    return this.req<T>("GET", `/categories`, { search: params?.search, type: params?.type, limit: params?.limit, skip: params?.skip }, undefined)
  }

  /** List document-extraction schemas */
  listDocSchemas<T = unknown>(params?: { search?: string; categoryId?: string; limit?: number; skip?: number }): Promise<T> {
    return this.req<T>("GET", `/schemas`, { search: params?.search, categoryId: params?.categoryId, limit: params?.limit, skip: params?.skip }, undefined)
  }

  /** List versions of a document schema */
  listDocSchemaVersions<T = unknown>(params: { schemaId: string; limit?: number; skip?: number }): Promise<T> {
    return this.req<T>("GET", `/schemas/${e(params.schemaId)}/versions`, { limit: params.limit, skip: params.skip }, undefined)
  }

  /** List document files */
  listFiles<T = unknown>(params?: { folder_id?: string; skip?: number; limit?: number; sortBy?: string; sortOrder?: string }): Promise<T> {
    return this.req<T>("GET", `/files`, { folder_id: params?.folder_id, skip: params?.skip, limit: params?.limit, sortBy: params?.sortBy, sortOrder: params?.sortOrder }, undefined)
  }

  /** List folders */
  listFolders<T = unknown>(params?: { ignore_assistant?: boolean }): Promise<T> {
    return this.req<T>("GET", `/folders`, { ignore_assistant: params?.ignore_assistant }, undefined)
  }

  /** List CRMBoard automations for the organization */
  listOrgCrmboards<T = unknown>(): Promise<T> {
    return this.req<T>("GET", `/v1/crmboard/organization/board`, undefined, undefined)
  }

  /** List related/candidate items for an item */
  listRelatedItems<T = unknown>(params: { boardId: string; id: string; relatedBoardId: string; link?: string; name?: string; skip?: number; limit?: number }): Promise<T> {
    return this.req<T>("GET", `/boards/${e(params.boardId)}/items/${e(params.id)}/related/${e(params.relatedBoardId)}`, { link: params.link, name: params.name, skip: params.skip, limit: params.limit }, undefined)
  }

  /** List saved segmentations for a board */
  listSegmentations<T = unknown>(params: { boardId: string }): Promise<T> {
    return this.req<T>("GET", `/boards/${e(params.boardId)}/segmentation`, undefined, undefined)
  }

  /** Run a read-only SPARQL query over a board's materialized graph */
  queryBoardSparql<T = unknown>(params: { boardId: string; depth?: number; format?: string; hydrate?: boolean; limit?: number; offset?: number; body: { query: string } }): Promise<T> {
    return this.req<T>("POST", `/boards/${e(params.boardId)}/sparql`, { depth: params.depth, format: params.format, hydrate: params.hydrate, limit: params.limit, offset: params.offset }, params.body)
  }

  /** Reorder a board's fields — @write */
  reorderBoardFields<T = unknown>(params: { id: string; body: { fieldIds: unknown[] } }): Promise<T> {
    return this.req<T>("POST", `/boards/${e(params.id)}/fields/reorder`, undefined, params.body)
  }

  /** Reorder boards — @write */
  reorderBoards<T = unknown>(params: { body: { boardIds: unknown[] } }): Promise<T> {
    return this.req<T>("POST", `/boards/reorder`, undefined, params.body)
  }

  /** Full-text search one board (org filter injected) */
  searchBoard<T = unknown>(params: { boardId: string; body: { q?: string; filter?: string; limit?: number; offset?: number; sort?: string } }): Promise<T> {
    return this.req<T>("POST", `/search/${e(params.boardId)}`, undefined, params.body)
  }

  /** Find CRMBoard automations. REQUIRES at least one of folder_id / board_id — calling with neither returns 400 */
  searchCrmboards<T = unknown>(params?: { folder_id?: string; board_id?: string }): Promise<T> {
    return this.req<T>("GET", `/v1/crmboard/search`, { folder_id: params?.folder_id, board_id: params?.board_id }, undefined)
  }

  /** Search document files */
  searchFiles<T = unknown>(params?: { q?: string; folder_id?: string; limit?: number }): Promise<T> {
    return this.req<T>("GET", `/files/search`, { q: params?.q, folder_id: params?.folder_id, limit: params?.limit }, undefined)
  }

  /** Search folders by name */
  searchFolders<T = unknown>(params?: { q?: string; limit?: number }): Promise<T> {
    return this.req<T>("GET", `/folders/search`, { q: params?.q, limit: params?.limit }, undefined)
  }

  /** Search across multiple boards (Meili-compatible) */
  searchMultiBoard<T = unknown>(params: { body: { queries: unknown[] } }): Promise<T> {
    return this.req<T>("POST", `/search/multi`, undefined, params.body)
  }

  /** ⚠️ DESTRUCTIVE — Unlink related items — @destructive */
  unlinkRelatedItems<T = unknown>(params: { boardId: string; id: string; body: { relatedBoardId: string; relatedItemIds: unknown[] } }): Promise<T> {
    return this.req<T>("DELETE", `/boards/${e(params.boardId)}/items/${e(params.id)}/related`, undefined, params.body)
  }

  /** Update a board (name, description, type, category, hidden) — @write */
  updateBoard<T = unknown>(params: { id: string; body: { name?: string; description?: string; type?: string; category?: string; hidden?: boolean } }): Promise<T> {
    return this.req<T>("PATCH", `/boards/${e(params.id)}`, undefined, params.body)
  }

  /** Update one field definition — @write */
  updateBoardField<T = unknown>(params: { id: string; fieldId: string; body: { name?: string; type?: string } }): Promise<T> {
    return this.req<T>("PATCH", `/boards/${e(params.id)}/fields/${e(params.fieldId)}`, undefined, params.body)
  }

  /** Update one item — @write */
  updateBoardItem<T = unknown>(params: { boardId: string; id: string; body: { fields?: Record<string, unknown> } }): Promise<T> {
    return this.req<T>("PATCH", `/boards/${e(params.boardId)}/items/${e(params.id)}`, undefined, params.body)
  }

  /** ⚠️ DESTRUCTIVE — Update every item matching a filter (mass mutation) — @destructive */
  updateBoardItemsByFilter<T = unknown>(params: { boardId: string; body: { filter: Record<string, unknown>; updates: Record<string, unknown> } }): Promise<T> {
    return this.req<T>("PATCH", `/boards/${e(params.boardId)}/items/update-by-filter`, undefined, params.body)
  }

  /** Update a CRMBoard automation — @write */
  updateCrmboard<T = unknown>(params: { id: string; body: { board_id: string } }): Promise<T> {
    return this.req<T>("PUT", `/v1/crmboard/board/${e(params.id)}`, undefined, params.body)
  }

  /** Update a document category — @write */
  updateDocCategory<T = unknown>(params: { id: string; body: { name?: string; parentId?: string } }): Promise<T> {
    return this.req<T>("PUT", `/categories/${e(params.id)}`, undefined, params.body)
  }

  /** Update a document schema — @write */
  updateDocSchema<T = unknown>(params: { id: string; body: { name?: string; description?: string } }): Promise<T> {
    return this.req<T>("PUT", `/schemas/${e(params.id)}`, undefined, params.body)
  }

  /** Update one attribute of a document schema — @write */
  updateDocSchemaAttribute<T = unknown>(params: { schemaId: string; attributeId: string; body: { change_note?: string } }): Promise<T> {
    return this.req<T>("PUT", `/schemas/${e(params.schemaId)}/attributes/${e(params.attributeId)}`, undefined, params.body)
  }

  /** Update a folder — @write */
  updateFolder<T = unknown>(params: { id: string; body: { name?: string } }): Promise<T> {
    return this.req<T>("PUT", `/folders/${e(params.id)}`, undefined, params.body)
  }

  /** Update a segmentation — @write */
  updateSegmentation<T = unknown>(params: { boardId: string; id: string; body: { name?: string; description?: string; filters?: unknown[] } }): Promise<T> {
    return this.req<T>("PUT", `/boards/${e(params.boardId)}/segmentation/${e(params.id)}`, undefined, params.body)
  }
}
